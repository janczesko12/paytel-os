import {BrowserMultiFormatReader,BarcodeFormat,DecodeHintType} from 'https://cdn.jsdelivr.net/npm/@zxing/browser@0.1.5/+esm';
const s=document.getElementById('status'),n=document.getElementById('name'),p=document.getElementById('price'),v=document.getElementById('video');
const hints=new Map();hints.set(DecodeHintType.POSSIBLE_FORMATS,[BarcodeFormat.EAN_13,BarcodeFormat.EAN_8,BarcodeFormat.UPC_A,BarcodeFormat.UPC_E,BarcodeFormat.CODE_128,BarcodeFormat.CODE_39]);
const r=new BrowserMultiFormatReader(hints);let last='';
async function send(code){const res=await fetch('/scan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({barcode:code})});const d=await res.json();if(d.success){s.textContent='✅';n.textContent=d.name;p.textContent=Number(d.price).toFixed(2)+' zł';navigator.vibrate?.(100);}else{s.textContent='Nie znaleziono';}}
(async()=>{const dev=await BrowserMultiFormatReader.listVideoInputDevices();await r.decodeFromVideoDevice(dev.at(-1)?.deviceId,v,(result)=>{if(result){const t=result.getText();if(t!==last){last=t;send(t);setTimeout(()=>last='',1200);}}});s.textContent='Skieruj kamerę na kod';})();