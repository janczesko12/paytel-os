# Render

1. Push repo to GitHub.
2. Render -> New -> Static Site.
3. Connect repository.
4. Build Command:
npm install && npm run build
5. Publish Directory:
dist
6. Deploy.

If using render.yaml Render should detect settings automatically.
